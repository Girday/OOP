# ЛАБОРАТОРНЫЕ РАБОТЫ ПО ООП

Репозиторий с лабораторными работами по курсу **"Объектно-ориентированное программирование"** (ООП).

Все работы выполнены на **C++17/20** с использованием современных практик разработки, CMake для сборки и GoogleTest для тестирования.

---

## Структура репозитория

| Работа | Тема | Основные концепции |
|--------|------|-------------------|
| [**hw1_var16**](#-hw1-алгоритм-евклида) | Алгоритм Евклида | Базовая структура проекта, unit-тесты |
| [**hw2_var6**](#-hw2-класс-five) | Класс Five (пятеричная система) | Правило пяти, управление памятью |
| [**hw3_var3**](#-hw3-наследование-и-полиморфизм) | Геометрические фигуры | Наследование, виртуальные функции, полиморфизм |
| [**hw4_var16**](#-hw4-шаблоны-и-метапрограммирование) | Шаблонные фигуры | Templates, concepts, метапрограммирование |
| [**hw5_var8**](#-hw5-итераторы-и-аллокаторы) | Однонаправленный список | Кастомные итераторы, PMR аллокаторы |
| [**hw6_var6**](#-hw6-паттерны-проектирования) | Игровая система NPC | Visitor, Observer, Factory |
| [**hw7_var12**](#-hw7-многопоточность) | Асинхронная игровая система | threads, mutex, atomic, многопоточность |

---

## 🔧 Сборка и запуск

### Требования
- **CMake** 3.10+
- **C++17/20** компилятор (GCC, Clang)
- **GoogleTest** (скачивается автоматически через FetchContent)

### Быстрый старт

Каждая работа собирается независимо:

```bash
cd hw1_var16
mkdir build && cd build
cmake ..
make

# Запуск основной программы
./HW1_VAR16

# Запуск тестов
ctest
```

### 🐳 Dev Container

Репозиторий содержит настроенный `.devcontainer` для VS Code:

```bash
# Откройте проект в VS Code
# Нажмите F1 → "Dev Containers: Reopen in Container"
```

---

## Описание лабораторных работ

### HW1: Алгоритм Евклида

**Задача:** Реализация алгоритма Евклида для нахождения НОД.

**Ключевые файлы:**
- `src/compile/gcd.cpp` — реализация алгоритма
- `src/test/basicTest.cpp` — базовые тесты
- `src/test/randomTest.cpp` — рандомизированные тесты

**Запуск:**
```bash
./HW1_VAR16  # Интерактивный ввод
echo "48 18" | ./HW1_VAR16  # НОД(48, 18) = 6
```

---

### HW2: Класс Five

**Задача:** Класс для работы с числами в пятеричной системе счисления.

**Реализованные функции:**
- ✅ Конструкторы (default, string, initializer_list)
- ✅ Правило пяти (копирование/перемещение)
- ✅ Арифметические операции (сложение, вычитание)
- ✅ Операции сравнения

**Пример использования:**
```cpp
Five a("243");      // 243₅
Five b{1, 2, 3};    // 321₅
Five sum = a.add(b);
sum.print(std::cout);  // 1014₅
```

**Тесты:** 38 unit-тестов покрывают все операции и граничные случаи.

---

### HW3: Наследование и полиморфизм

**Задача:** Иерархия геометрических фигур с полиморфным поведением.

**Фигуры (Variant 3):**
- **Rectangle** — прямоугольник
- **Rhombus** — ромб
- **Trapezoid** — трапеция

**Возможности:**
- Вычисление площади через `operator double()`
- Нахождение геометрического центра
- Динамический массив фигур
- Сравнение фигур через `operator==`

**Интерактивное меню:**
```bash
./HW3_VAR3
# Добавление, удаление, вывод фигур
```

---

### HW4: Шаблоны и метапрограммирование

**Задача:** Обобщённые геометрические фигуры с использованием templates и concepts.

**Фигуры (Variant 16):**
- **Octagon** — правильный восьмиугольник
- **Triangle** — равнобедренный треугольник
- **Square** — квадрат

**Ключевые концепции:**
```cpp
template <Scalar T>  // Concept для scalar типов
class Point { T x, y; };

template <Scalar T>
class Figure { /* ... */ };

// Можно использовать int, float, double:
Array<Square<double>> squares;
Array<Triangle<int>> triangles;
```

**Фичи:**
- Обобщённый контейнер `Array<T>`
- Move-семантика
- Полиморфизм через `shared_ptr<Figure<T>>`

---

### HW5: Итераторы и аллокаторы

**Задача:** Реализация `ForwardList` с кастомным аллокатором памяти.

**Компоненты:**
- `ForwardList<T>` — однонаправленный список
- `ListIterator<T>` — STL-совместимый итератор
- `FixedBlockResource` — PMR аллокатор с фиксированным буфером

**Особенности аллокатора:**
```cpp
FixedBlockResource mem(2048);  // 2KB буфер
ForwardList<int> list(&mem);   // Использует наш аллокатор

// Переиспользование памяти после удаления
{
    ForwardList<int> temp(&mem);
    temp.push_back(100);
} // Память возвращается в пул
```

**Range-based for поддержка:**
```cpp
for (const auto& value : list) {
    std::cout << value << " ";
}
```

---

### HW6: Паттерны проектирования

**Задача:** Игровая система с боевой механикой через паттерны проектирования.

**NPC (Variant 6):**
- **Bear** (Медведь) — убивает эльфов
- **Elf** (Эльф) — убивает разбойников
- **Bandit** (Разбойник) — убивает разбойников

**Реализованные паттерны:**

| Паттерн | Назначение |
|---------|-----------|
| **Factory** | Создание NPC по типу |
| **Visitor** | Реализация правил боя (двойная диспетчеризация) |
| **Observer** | Логирование событий боя (TextObserver, FileObserver) |

**Пример работы:**
```bash
./HW6_VAR6
# Генерируется 100 случайных NPC
# Проводится симуляция боёв на разных дистанциях
# Результаты сохраняются в log.txt
```

---

### HW7: Многопоточность

**Задача:** Асинхронная игровая симуляция с использованием потоков.

**Архитектура:**

```
┌─────────────┐    ┌──────────────┐    ┌─────────────┐
│ Move Thread │───▶│ FightManager │◀───│ Fight Thread│
└─────────────┘    └──────────────┘    └─────────────┘
                           │
                           ▼
                   ┌──────────────┐
                   │ Print Thread │
                   └──────────────┘
```

**Потоки:**
1. **Movement Thread** — перемещение NPC по карте (координаты: 50×50)
2. **Fight Thread** — обработка очереди боёв
3. **Print Thread** — вывод карты каждую секунду (25×25 grid)

**Характеристики NPC:**

| Тип | Дистанция движения | Дистанция атаки |
|-----|--------------------|-----------------|
| Bear | 5 | 10 |
| Elf | 10 | 50 |
| Bandit | 10 | 10 |

**Синхронизация:**
- `std::mutex` для thread-safe операций
- `std::atomic<bool>` для флага работы симуляции
- `std::queue` для событий боя

**Визуализация:**
```
| | | | |E| | |R| | |
| |M| | | | | | | | |
| | | | | | |E| | | |
...
========================
🟡 Медведи: 15 | 🟢 Эльфы: 12 | 🔴 Разбойники: 8 | Всего: 35/50
```

**Запуск симуляции:**
```bash
./HW7_VAR6
# Симуляция длится 30 секунд
# Карта обновляется каждую секунду
# Лог боёв сохраняется в log.txt
```


## Тестирование

Все работы содержат комплексные unit-тесты с использованием **GoogleTest**:

```bash
# В директории build/
ctest

# Или напрямую
./gtests
```

**Покрытие тестами:**
- ✅ Базовые операции
- ✅ Граничные случаи
- ✅ Обработка ошибок
- ✅ Стресс-тесты
- ✅ Интеграционные тесты


## Замечания

- **Варианты заданий** различаются для каждой работы (указаны в названиях hw*_var*)
- Все проекты следуют **единому стилю** кодирования
- Используется **современный C++** (RAII, smart pointers, move semantics)
- **CMake** позволяет легко интегрировать проекты в IDE


## Лицензия

Этот репозиторий создан в учебных целях. Код может свободно использоваться для изучения и вдохновения.

<div align="center">

## 

**Григорьев Максим** • [@Girday](https://github.com/Girday)

Если код помог тебе понять что-то новое, поставь ⭐!

</div>